# TODO:
# Ingress resource configured correctly and application accessible at
# 'http://iron-gallery-braavos.com:30099/'
# xIngress Resource, name: 'iron-gallery-ingress'
# host: iron-gallery-braavos.com
# http parth: '/'
# http backend serviceName: 'iron-gallery-service'
# Name: ingress-spacehttp backend servicePort: '80'
#
# Maybe this is how we're supposed to get the nodePort to show up?
---
apiVersion: v1
kind: Service
metadata:
  name: gallery-of-braavos
spec:
  selector:
    name: iron-gallery
  ports:
  - targetPort: 80
    port: 80
    nodePort: 30099
    protocol: TCP
#
# This gets everything except for the first TODO
# Apparently the lab is still running v1.11.3 so we're using the older apiVersion
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: iron-gallery-ingress
spec:
  rules:
  - host: iron-gallery-braavos.com
    http:
      paths:
      - path: /
        backend:
          serviceName: iron-gallery-service
          servicePort: 80
